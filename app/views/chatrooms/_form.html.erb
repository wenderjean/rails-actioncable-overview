<%= simple_form_for @chatroom do |f| %>
	<%= f.input :topic, autofocus: true %>
	<%= f.submit 'Create', class: 'btn btn-default' %>
  <%= link_to 'Back', chatrooms_url, class: 'btn btn-default' %>
<% end %>
